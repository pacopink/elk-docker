Use this repo you can easily setup an ELK environment using docker.
If you want to change the version of ELK components, modify 'VERSION' file

Usage:

Run below commands in order

```bash
./es/run.sh
./kibana/run.sh
./logstash/run.sh
```
